{
  "name": "wahtson",
  "version": "0.1.0",
  "main": "src/bot.js",
  "bin": "src/bot.js",
  "dependencies": {
    "chalk": "^3.0.0",
    "discord.js": "^11.5.1",
    "open": "^7.0.0",
    "toml": "^3.0.0"
  },
  "devDependencies": {
    "pkg": "^4.4.2"
  },
  "scripts": {
      "build": "pkg . --out-path build && cp config-example.toml build/config.toml"
  }
}
